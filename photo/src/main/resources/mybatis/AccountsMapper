<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.test.mapper.AccountsMapper">
    <select id="join" resultType="com.example.photologger.photo.domain.User">
        INSERT INTO user VALUES(idx, email, password, name, nickname, phonenumber, sex, profile_img_loc, year, month, day, cornfirm, type, type, gall_count, user_subscribe_count) values (1, #{email}, #{password}, #{name}, #{nicName}, #{phonenumber}, #{sex}, #{profile_img_loc}, #{year}, #{month}, #{day}, #{cornfirm}, #{type}, #{gall_count}, #{user_subscribe_count})
    </select>
</mapper>